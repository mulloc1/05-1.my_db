평가기준 1 : Pass

사유: - ERD 및 스키마 설명에 따르면 category, book, member, rental의 최소 4개 테이블이 존재함을 명시하고 있습니다.

각 테이블에 PK가 정의되어 있음은 SQL 파일(01_schema.sql)에 구현되어 있다고 기재되어 있습니다.
평가기준 2 : Pass

사유: - category-book, book-rental, member-rental 등 1:N 관계가 3개 존재하여 최소 2개 이상 기준을 충족합니다.

Verifying Constraints 섹션에서 없는 값 참조 시 FOREIGN KEY constraint failed 에러가 발생함을 증명하고 있습니다.
평가기준 3 : Pass

사유: - Subject Mapping 항목에서 Sample data (≥ 10 rows/table) 기준을 충족하여 02_seed.sql에 작성되었음을 명시하고 있습니다.

평가기준 4 : Pass

사유: - 15 Queries at a Glance 표에서 **Basic select(4개), JOIN(4개), Aggregate(3개), Subquery(1개), UPDATE/DELETE(2개), Index(1개)**로 구성된 15개의 쿼리가 정확히 작성되어 있음을 확인할 수 있습니다.

평가기준 5 : Pass

사유: - 15개의 쿼리 목록 표에 각 결과물에 대한 스크린샷 파일 경로(Capture 열)가 첨부되어 있습니다.

평가기준 6 : Fail

사유: - 제출된 결과물(README)에는 테이블 간의 관계(ERD)는 나열되어 있으나, 테이블을 왜 이렇게 나눴는지, 각 테이블의 구체적인 역할이 무엇인지에 대한 설명이 누락되어 있습니다.

평가기준 7 : Fail

사유: - 1:N 관계의 목록은 제시되었으나, 실제 도메인에서 어떤 의미인지 예시를 들어 구두로 설명할 수 있는지 여부는 제출된 문서만으로는 확인 불가합니다.

평가기준 8 : Fail

사유: - 컬럼 타입(TEXT, INTEGER, DATE 등)을 왜 그렇게 선택했는지에 대한 설명이나 이유가 문서상에 포함되어 있지 않습니다.

평가기준 9 : Fail

사유: - rental(member_id, rented_at)에 인덱스를 생성했다는 내용은 있으나, 왜 그 컬럼이어야 하는지 이유는 기재되어 있지 않습니다.

평가기준 10 : Fail

사유: - 데이터베이스가 엑셀과 무엇이 다른지, 왜 테이블을 나눠서 저장하는지 비교하며 설명하는 내용이 없습니다.

평가기준 11 : Fail

사유: - 본인 스키마 기준으로 PK와 FK의 역할을 구분하고 데이터를 어떻게 연결하는지 설명하는 내용이 부족합니다.

평가기준 12 : Fail

사유: - INNER JOIN과 LEFT JOIN을 사용한 쿼리가 존재하나, 실행 결과를 보며 두 JOIN의 차이를 설명하는 내용이 없습니다.

평가기준 13 : Fail

사유: - 집계 함수를 사용한 쿼리는 존재하지만, 어떻게 동작하는지 쿼리 결과를 보며 이야기하는 설명이 포함되어 있지 않습니다.

평가기준 14 : Fail

사유: - 작성한 쿼리 중 가장 복잡했던 쿼리를 선택하고 단계별로 어떻게 풀었는지 설명하는 내용이 확인되지 않습니다.

평가기준 15 : Fail

사유: - 미션 수행 중 가장 어려웠던 부분과 해결 방법을 구체적으로 작성한 회고 내용이 없습니다.
